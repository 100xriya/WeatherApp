# Weather App

A simple and responsive weather application built with HTML, Tailwind CSS, and vanilla JavaScript. The app uses the OpenWeather API to fetch and display real-time weather information for a city entered by the user.

## Features

* 🌍 Search weather by city name
* 🌡️ Display current temperature in Celsius
* ☁️ Display current weather conditions
* ⚠️ Handle invalid city names and API errors
* 📱 Responsive user interface
* 🔄 Fetch real-time weather data using an API

## Technologies Used

* HTML
* Tailwind CSS
* JavaScript
* OpenWeather API

## What I Learned

* Working with APIs
* Using `fetch()` to make HTTP requests
* Understanding Promises and `async/await`
* Handling API responses with `response.json()`
* Checking HTTP response status with `response.ok`
* Handling errors with `try...catch`
* Working with JSON data
* Destructuring objects
* Accessing nested objects and arrays
* Using template literals to construct API URLs
* Dynamically updating the DOM

## Future Improvements

* Add animated weather icons based on weather conditions
* Different icons for day and night
* Add location-based weather detection
* Add a multi-day weather forecast
* Display additional information such as humidity, wind speed, and feels-like temperature
* Add Celsius/Fahrenheit unit switching
* Improve loading and error states
* Further improve the UI and animations

## Project Status

🚧 **In Progress**

This project is being built as part of my JavaScript learning journey, with new features and improvements being added as I learn more.
